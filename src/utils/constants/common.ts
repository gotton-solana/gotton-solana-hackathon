export const PROVIDERS = {
  REDIS: 'redis',
};

export const API_MAX_RETRY = 5;
export const API_TIMEOUT = 30000; // 30s
export const GET_API_TIMEOUT = 5000; // 5s

export const NODE_ENV = {
  PRODUCTION: 'production',
};
